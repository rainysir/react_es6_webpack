#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rstool-build/bin/rstool-build" "$@"
  ret=$?
else 
  node  "$basedir/../rstool-build/bin/rstool-build" "$@"
  ret=$?
fi
exit $ret
